To compile the programs in this project for debugging, type:
```
nimble build -d:ssl
```

To compile a release, type:
```
nimble build -d:ssl -d:release
```

TODO: make ```-d:ssl``` apply only to the compilation of nimsend